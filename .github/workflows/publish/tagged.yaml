name: Tagged release

on:
  push:
    # tags:
      # - 'v[0-9]+.[0-9]+.[0-9]+'
    branches:
      - TT-9616
jobs:
  publish:
    name: Publish to SVN
    runs-on: gha-internal-amd64
    steps:
      - run: |
          sudo apt-get install subversion -y
      - run: |
          svn --version
